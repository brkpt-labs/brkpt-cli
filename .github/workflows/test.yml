name: Tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ "*" ]

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup
      - name: Run e2e tests
        run: pnpm test
